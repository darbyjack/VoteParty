package me.clip.voteparty

/*
import com.sxtanna.korm.Korm
import me.clip.voteparty.conf.base.Config
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

object ConfigMergeTest
{
	
	private val DEF_PREF = Preference(
		"en_US",
		"HypeMan"
	                                 )
	
	private val DEF_TEXT =
		"""
			customLang: "en_US"
		""".trimIndent()
	
	data class Preference(var customLang: String?,
	                      var customName: String?) : Config
	
	
	private val korm = Korm()
	
	
	@Test
	internal fun testMerge()
	{
		assertEquals(DEF_PREF, korm.pull(DEF_TEXT).to<Preference>()?.apply { merge(DEF_PREF) })
	}
	
	
}*/
